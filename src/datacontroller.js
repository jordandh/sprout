define("datacontroller", ["util", "base"], function (_, base) {
    "use strict";
    
    /**
     * @class datacontroller
     * A data controller provides functionality for accessing and manipulating a collection or model.
     * @extends base
     */
    return base.extend({
    });
});